/*! jQuery v3.1.0 | (c) jQuery Foundation | jquery.org/license */
var swiper = new Swiper(".flag-slider", {
    slidesPerView: 1,
    loop: true,
    autoplay: {
        delay: 3000,
    },
    navigation: {
        nextEl: '.slider-button-next',
        prevEl: '.slider-button-prev',
    },
    breakpoints: {
        600: {
            slidesPerView: 2,
        },
        768: {
            slidesPerView: 3,
        },
        1024: {
            slidesPerView: 4,
        },
        1200: {
            slidesPerView: 5,
        },
    },
});
$('#circle').circleProgress({
    value: 0.85,
    size: 250,
    lineCap: "round",
    animation: false,
    fill: {
        gradient: ["#579cf4", "#2042cf"]
    }
});
//Check to see if the window is top if not then display button
$(window).scroll(function () {
    // Show button after 100px
    // var showAfter = 800;
    if ($(this).scrollTop() > 100) {
        $('.back-to-top').fadeIn();
    } else {
        $('.back-to-top').fadeOut();
    }
});
//Click event to scroll to top
$('.back-to-top').click(function () {
    $('html, body').animate({
        scrollTop: 0
    }, 800);
    return false;
});
// tab next and prev button function here ....
var currentTab = 0;
$(function () {
    $("#tabs").tabs({
        select: function (e, i) {
            currentTab = i.index;
        }
    });
});
$("#btnNext").click(function () {
    var tabs = $('#tabs').tabs();
    var c = $('#tabs').tabs("length");
    currentTab = currentTab === (c - 1) ? currentTab : (currentTab + 1);
    tabs.tabs('select', currentTab);
    $("#btnPrevious").show();
    if (currentTab === (c - 1)) {
        $("#btnNext").show();
    } else {
        $("#btnNext").show();
    }
});
$("#btnPrevious").click(function () {
    var tabs = $('#tabs').tabs();
    var c = $('#tabs').tabs("length");
    currentTab = currentTab === 0 ? currentTab : (currentTab - 1);
    tabs.tabs('select', currentTab);
    if (currentTab === 0) {
        $("#btnNext").show();
    }
    if (currentTab < (c - 1)) {
        $("#btnNext").show();
    }
});
